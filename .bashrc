alias gcm="git commit -m"
alias gcmmu="git commit -m misc-update"
alias gp="git push"
alias gu="git add -u"
alias ac="$( $(gu) git commit -m auto-commit) && git push "

alias sb="source ~/.bashrc"
alias edbrc="vim ~/.bashrc && sb"

alias ls="exa"

alias t="echo test"
